export type pizza = {
    id?: string,
    imageUrl: string,
    title: string,
    types?: [number],
    sizes?: [number],
    price: number,
    category?: number,
    description?: string,
    count: number,
}